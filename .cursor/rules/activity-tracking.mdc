---
description: Activity tracking system — device fingerprinting, session tracking, owner-only UI
globs: src/utils/activityLogger.ts
alwaysApply: false
---

# Activity Tracking System

## Overview

Silent, transparent tracking of non-admin user activity. Admin sessions are excluded.
Data stored in `activity_log` table with `group_id` isolation via RLS. Only the group owner can view activity.

## Files

- **Logger**: `src/utils/activityLogger.ts` — session init, updates, fingerprinting, Supabase writes
- **Hooks**: `src/App.tsx` — starts tracking on login, updates on navigation + `visibilitychange`
- **UI**: `src/screens/SettingsScreen.tsx` — owner-only "Activity" tab with device cards
- **Type**: `ActivityLogEntry` + `DeviceFingerprint` in `src/types/index.ts`

## Device Identification

Each browser gets a persistent UUID (`poker_device_id` in localStorage). Combined with fingerprint:

| Signal | Source | Identifies |
|--------|--------|-----------|
| GPU renderer | WebGL `UNMASKED_RENDERER_WEBGL` | Exact phone model |
| CPU cores | `navigator.hardwareConcurrency` | Hardware class |
| RAM | `navigator.deviceMemory` | Device tier |
| Touch points | `navigator.maxTouchPoints` | Phone vs tablet vs desktop |
| Language | `navigator.language` | System locale |
| Canvas hash | Canvas rendering fingerprint | Unique per device/GPU |

Admin labels devices with player names in Activity tab (`poker_device_labels` in localStorage).

## Session Lifecycle

1. **Start**: `logActivity(role)` on login (non-admin only)
2. **Updates**: `updateSessionActivity(screens, duration)` every 5 minutes + on navigation; skips if unchanged
3. **Tab close**: `visibilitychange` fires with `keepalive: true`
4. **Sign out**: Final update + `resetSession()`

## Functions (all Supabase-only)

- `logActivity` → INSERT into `activity_log`
- `updateSessionActivity` → UPDATE by `device_id` + `timestamp`
- `fetchActivityLog` → SELECT with group_id filter, ORDER BY timestamp DESC, LIMIT 200
- `deleteActivityEntry` → DELETE by `device_id` + `timestamp`
- `deleteDeviceEntries` → DELETE by `device_id`
- `clearActivityLog` → DELETE by `group_id`

## Cleanup

`cleanup_old_activity(days)` RPC in `006-supabase-improvements.sql` — owner-only, scoped to own group.
