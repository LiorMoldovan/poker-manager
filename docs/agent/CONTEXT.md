# CONTEXT — Current State

> **What this is**: A 30-second orientation for the agent at the start of a chat. Refreshed in place (overwrite, not append). If something here is stale, fix it before doing other work.
> **Last refreshed**: 2026-05-13 (post v5.60.2 — photo chip-counting tip-vs-pipeline contradiction repaired + defensive same-color sum in applyPhotoResult + Hebrew instruction polish)

---

## Right now

- **Version on `origin/main`**: `5.60.2` — three fixes around the photo chip-counting feature, all rooted in the v5.49→v5.59 pipeline shift that wasn't fully propagated to user-facing copy or downstream code. (1) **Tip-vs-pipeline contradiction**: the `chips.photo.tip.maxStack` tip on the secondary tips screen still said "split tall stacks into multiple stacks of the same color, up to 10 chips per stack" — written for the v5.49 multi-shot consensus era when multiple same-color stacks were summed. The v5.59 rebuild changed that: now each detected stack maps 1:1 to a chipId via HSL distance, and `applyPhotoResult` overwrites per-chipId. Following the tip's advice silently halved (or worse) the user's count for any color split into multiple piles. Rewrote both HE and EN to say "one stack per color, any height (up to ~25 chips), a single stack is more accurate than several small ones" — matches `MAX_REASONABLE_COUNT = 30` in `chipGeometry.ts` and the actual happy path. (2) **Defensive `applyPhotoResult` aggregation in `ChipEntryScreen.tsx`**: even with corrected guidance, a user who accidentally splits chips (or runs out of vertical space) shouldn't lose data silently. Added a `summedByChipId` Map between `result.stacks` iteration and `updateChipCount` writes — sums counts across stacks sharing a chipId before writing. Happy path (one stack per color) is bit-identical: each chipId appears exactly once, sum equals single value. `userEditedFields` skip preserved per chipId. NOT applied to the test card flow in `SettingsScreen.tsx` line 4440 — that surface has per-stack feedback semantics for `chip_count_feedback` row submission and changing it without re-thinking the row shape was deemed higher risk than the existing minor display oddity. (3) **Hebrew copy polish**: `chips.photo.instructionStep3` future tense → imperative (`ימלאו את הפריים` → `מלאו את הפריים בערימות הקופה`, matches the `מיינו` / `צלמו` imperative pattern of the other steps); `chips.photo.instructionStep4` noun-phrase → sentence (`תוצאה אוטומטית` → `הספירה אוטומטית`).
- **Previous version**: `5.60.1` — small chip-entry UX patch on top of v5.60.0. (1) Photo chip-count button was unreachable in the admin manual flow because `selectPlayer()` auto-opens the numpad modal at chip 0, skipping the per-player overview where the standalone photo button lives — added a 📷 escape-hatch icon to the numpad's green player-name header bar (only renders when `photoAvailable && !photoResults[playerId]`). Tap → numpad closes, photo modal opens for the same player, user lands on overview with AI banner. Already-typed values are preserved by the existing `userEditedFields` skip in `applyPhotoResult`. (2) Chip-card +/− DOM order swapped from `[minus, input, plus]` to `[plus, input, minus]` — under inherited RTL the row now renders visually as `[− | count | +]` (minus left, plus right) instead of the previous `[+ | count | −]`. (3) Hebrew `chips.numpadNext` changed from `'הבא →'` to `'הבא ←'` so the next-button arrow points in the same direction the dots progress (RTL flex puts active dot at chipIndex=0 on the right; advancing moves it leftward, so `←` is the correct "forward" arrow in RTL). English `'Next →'` kept as-is — document direction is LTR for English locale (`App.tsx` line 1553), so dots progress LTR and `→` matches.
- **Previous version**: `5.60.0` (commit `9b39e40`) — two cohesive UX streams merged together. (1) Schedule promoted from `Settings → Schedule tab` to a dedicated top-level `/schedule` route. All vote deep-links updated: emails (`src/utils/scheduleNotifications.ts`), push notifications (`api/notification-worker.ts`), in-app vote banner (`src/components/VoteReminderBanner.tsx`), OAuth redirect (`src/hooks/useSupabaseAuth.ts`). Legacy `/settings?tab=schedule` URLs get a transparent redirect in `SettingsScreen.tsx` so old emails and push payloads keep working. Empty state on home is role-aware (admin: "open the next poll" amber CTA / member: "next vote opens soon") AND auto-schedule-aware (shows the concrete next dayName/date/time when the group has `scheduleAutoCreateEnabled`). (2) Single-date open polls now render the same rich `renderRichDateCard` helper that State 2 (confirmed) uses — date + countdown + location + `מגיעים: …` attendee list — instead of the lean glance-rows view. Auto-elevates to `🎯 ערב פוקר נקבע` the moment seats fill up (`elevateWhenFilled: true` gate, simplified to `missingSeats === 0 && comingNames.length > 0` to be robust against stale player-cache windows). Non-voters get an amber `מחכים להצבעה שלך · ` subtitle prefix, suppressed once elevated. Empty single-date polls show `עדיין אין הצבעות - לחצו כדי להצביע` CTA body. Honest title rules: title always reflects actual poll state — `הצבעה פתוחה` for open, `ערב פוקר נקבע` for pinned-or-de-facto-filled.

- **Open follow-up — admin chip-entry photo discoverability**: The 📷 escape hatch in the numpad header is currently the ONLY way admins can reach the photo flow on `ChipEntryScreen` (the standalone overview button at line 1082-1106 is unreachable because `selectPlayer` auto-jumps straight to the numpad). Lior accepted this as the right trade-off for now while photo accuracy is at v5.59.0 levels (60–80% per-stack without selfies) — the manual flow is the common case and should stay byte-identical, photo is the opt-in escape hatch. If/when photo accuracy improves enough that most users prefer it, re-evaluate and consider promoting to a first-class "manual or photo?" chooser before the numpad opens. This shapes the design choice in the numpad header: 📷 is intentionally subtle (just an icon button in the green bar), not a big CTA. — two cohesive UX streams merged together. (1) Schedule promoted from `Settings → Schedule tab` to a dedicated top-level `/schedule` route. All vote deep-links updated: emails (`src/utils/scheduleNotifications.ts`), push notifications (`api/notification-worker.ts`), in-app vote banner (`src/components/VoteReminderBanner.tsx`), OAuth redirect (`src/hooks/useSupabaseAuth.ts`). Legacy `/settings?tab=schedule` URLs get a transparent redirect in `SettingsScreen.tsx` so old emails and push payloads keep working. Empty state on home is role-aware (admin: "open the next poll" amber CTA / member: "next vote opens soon") AND auto-schedule-aware (shows the concrete next dayName/date/time when the group has `scheduleAutoCreateEnabled`). (2) Single-date open polls now render the same rich `renderRichDateCard` helper that State 2 (confirmed) uses — date + countdown + location + `מגיעים: …` attendee list — instead of the lean glance-rows view. Auto-elevates to `🎯 ערב פוקר נקבע` the moment seats fill up (`elevateWhenFilled: true` gate, simplified to `missingSeats === 0 && comingNames.length > 0` to be robust against stale player-cache windows). Non-voters get an amber `מחכים להצבעה שלך · ` subtitle prefix, suppressed once elevated. Empty single-date polls show `עדיין אין הצבעות - לחצו כדי להצביע` CTA body. Honest title rules: title always reflects actual poll state — `הצבעה פתוחה` for open, `ערב פוקר נקבע` for pinned-or-de-facto-filled.
- **No in-flight WIP**: working tree clean after v5.60.0 push. 9 CRLF-ghost-modified files remain in `git status` (PhotoCaptureModal, TriviaReportsTab, supabaseCache, ChipEntryScreen, TriviaGameScreen, types/index, geminiAI, triviaGenerator, triviaReportNotifications) — `git diff --numstat` confirms 0/0 line delta on all of them, pure CRLF noise. Will resolve themselves whenever someone genuinely edits one.
- **Open follow-up (deferred — needs user screenshot to investigate)**: Lior reported a fuzzy memory of a "mini table with more details" on the home card. Couldn't pin down which view (schedule page tiles? State 3 multi-date glance rows? something new?). Punted to a future session — wait for a screenshot rather than chase the memory.
- **Open follow-up — local dev shows zero admin controls in LiveGameScreen**: Lior reported via DOM dump that his local dev environment shows the live game screen WITHOUT any admin-only controls (no 💀 🔥 ☕ buttons, no add-player button, no rebuy/half-buyin buttons per player, no remove-player ✕, no add-expense button, no end-game footer). Production works fine. This means `isAdmin` (line 69, `role === 'admin' || isSuperAdmin || isOwner`) is resolving to false in his local browser session — almost certainly a different/non-owner account logged in on localhost vs his phone. Asked him to confirm which user/group his local dev is using. If it turns out to be a real permission-resolution bug rather than a wrong-account situation, dig into `usePermissions()` resolution timing (the page rendered the full layout, so it's past the initial loading gate — defaults of `role='member'` + `isOwner=false` would match the symptom exactly if membership fetch is failing silently). Punted pending his confirmation.
- **Applied SQL migrations** (no new ones this session):
  - `074-chip-selfies.sql` — `selfie_base64 TEXT NULL` + `selfie_dominant_hex TEXT NULL` on `chip_values`. Pipeline falls back to `displayColor` when missing.
  - `075-chip-feedback-pipeline-meta.sql` — `pipeline_meta JSONB NULL` on `chip_count_feedback`. Stores `{whiteBalanceApplied, detectionSignal, totalValueCheckResult}` per row; legacy rows have NULL and skip the new KPIs in the dashboard aggregator.
- **Earlier-still SQL** (still relevant, do not re-apply):
  - `070-chip-count-tuning.sql` (v5.55), `071-chip-feedback-admin-read.sql` (admin SELECT on feedback, v5.58), `072-trivia-leaderboard-sort-by-accuracy.sql` (v5.58), `073-trivia-deleted-templates.sql` (super-admin kill-switch, v5.58).
- **Vercel env vars (no change)**: `WORKER_INTERNAL_SECRET`, `SUPABASE_SERVICE_ROLE_KEY`, `OWNER_GROUP_ID`, plus per-feature keys (Gemini/ElevenLabs/EmailJS).
- **Notification dispatch (still as of v5.49.x architecture)**: fully server-side. Every notification flows through `notification_jobs` → pg_net `net.http_post` trigger → `/api/notification-worker` Edge Function → `/api/send-push` + `/api/send-email`. Browser worker (`src/utils/notificationWorker.ts`) is redundant fallback only. DB triggers from 066: `trg_enqueue_vote_change_on_vote` on `game_poll_votes`, `trg_enqueue_trivia_report_on_insert` + `trg_enqueue_trivia_report_on_resolve` on `trivia_reports`, plus the `trg_http_dispatch_notification_job` webhook on `notification_jobs`. pg_cron job `notification-jobs-sweep` runs every minute as the retry path.

## Chip-counting permission model (v5.58.0 → v5.59.0 unchanged)

The Services tab (`'ai'`) is admin-accessible:

| Card | Visible to | Why |
|------|-----------|-----|
| Game Readiness, AI Usage, API Keys, key setup guide, share-photos opt-in | Owner only (`{isOwner && (...)}`) | API/billing config + privacy decisions |
| Photo Chip Counting Test Card | All admins | Co-tester needs to take photos |
| Accuracy Dashboard (KPIs, per-color, trend chart) | All admins (`isOwner || role==='admin' || isSuperAdmin`) | RLS migration 071 lets admins SELECT `chip_count_feedback` |
| Tune button + Revert button | Owner only (read-only hint shown to admins) | RLS on `chip_count_tuning_overrides` INSERT is owner-only — keeps DB and UI aligned |
| Auto-rollback safety net (`checkAndAutoRollbackIfNeeded`) | Owner only (gated in load `useEffect`) | It writes a revert row → owner-only RLS would reject for admins |

In Lior's group (`Poker Night`, owner = Lior `2c8d9de2-…`): admins are Lior, אייל (`eyalerz@gmail.com`), חרדון. Both non-owner admins will get test-card + dashboard access; only Lior can trigger tunings.

## Spot-check queries

For the in-app feedback loop (what's coming in from real games):

```sql
SELECT created_at, player_name, model_used, overall_confidence,
       shots_used, total_stacks, correct_stacks, total_chip_delta, total_abs_delta
FROM   public.chip_count_feedback
ORDER BY created_at DESC LIMIT 20;
```

Healthy = rows showing up after games where the photo button was used; `correct_stacks` ≈ `total_stacks` for in-tolerance counts; `total_chip_delta` skewed positive ⇒ AI still undercounting; near zero ⇒ MAX aggregation worked. Per-stack diffs live in the `stacks` JSONB column for deeper drill-down (`SELECT stacks FROM chip_count_feedback WHERE id = '…'`).

For the notification system (still relevant):

```sql
SELECT id, kind, status, attempts, last_error, claimed_at, completed_at, created_at
FROM public.notification_jobs ORDER BY created_at DESC LIMIT 10;
```

Healthy = all `done` with `attempts=1`, `last_error=null`. `pending` rows older than ~90s mean pg_cron sweep isn't draining (check `cron.job` and `net._http_response` for the failure mode). `failed` after `attempts=3` means the worker rejected the job three times.

## Spot-check query for live notification health

If anything ever feels off, run this in the SQL editor:

```sql
SELECT id, kind, status, attempts, last_error, claimed_at, completed_at, created_at
FROM public.notification_jobs ORDER BY created_at DESC LIMIT 10;
```

Healthy = all `done` with `attempts=1`, `last_error=null`. `pending` rows older than ~90s mean pg_cron sweep isn't draining (check `cron.job` and `net._http_response` for the failure mode). `failed` after `attempts=3` means the worker rejected the job three times — `last_error` will say why.

## Active themes (last ~10 versions)

- **v5.60.2 — Photo chip-count tip/pipeline alignment + defensive same-color sum** (2026-05-13): Same-day follow-up to v5.60.1 after Lior took a real test photo and asked clarifying questions about the on-screen instructions. Investigation revealed a tip on the secondary tips screen (`chips.photo.tip.maxStack`) was actively breaking user counts — it dated from the v5.49 multi-shot consensus era and told users to "split tall stacks into multiple stacks of the same color", but the v5.59 rebuild pipeline doesn't sum same-color stacks (each detected stack maps 1:1 to a chipId, `applyPhotoResult` overwrites per-chipId). Users following the tip lost half or more of their count, silently. Three fixes: (1) Rewrote the tip in HE+EN to say "one stack per color, any height (up to ~25 chips), a single stack is more accurate than several small ones" — matches `MAX_REASONABLE_COUNT = 30` in `chipGeometry.ts`. (2) Defensive sum in `applyPhotoResult` (`ChipEntryScreen.tsx` line ~544): aggregates `result.stacks` by `chipId` via a `summedByChipId` Map before writing to `chipCounts`, so users with old habits or weird splits get correct totals. Happy path (one stack per color) is bit-identical — each chipId appears exactly once, sum equals single value. `userEditedFields` skip preserved per chipId. NOT applied to `SettingsScreen.tsx` test-card line 4440 because that surface has per-stack feedback row semantics; changing it without rethinking the row shape was deemed higher risk than the existing display oddity, and the test card is debug-only. (3) Hebrew polish: `instructionStep3` future-tense `ימלאו` → imperative `מלאו את הפריים בערימות הקופה` (matches `מיינו` / `צלמו` pattern); `instructionStep4` noun-phrase `תוצאה אוטומטית` → sentence `הספירה אוטומטית`. Considered also tweaking `instructionStep2` ("מהצד") and adding an explicit "don't split into 5s" sentence — Lior pushed back on both as unnecessary noise (the secondary tips screen's `tip.angle` already clarifies 45°-from-side; the visualization below step 1 shows one chip per color which already implies "one per color"). Trust the user's instinct for what's worth saying.
- **v5.60.1 — Chip-entry UX polish** (2026-05-13): Three small fixes on `ChipEntryScreen`. (1) The photo chip-count button was orphaned for admins — `selectPlayer()` auto-opens the numpad modal on tap, skipping the per-player overview where the standalone button at lines 1082-1106 lives. Added a 📷 icon to the numpad's green player-name header bar that closes the numpad and opens the photo modal for the same player; already-typed counts are preserved by the existing `userEditedFields` skip in `applyPhotoResult`. Gated identically to the overview button: `photoAvailable && !photoResults[playerId]`. Considered three alternatives (overview-first / chooser dialog / inline-button) and picked button-in-numpad after Lior pointed out that with photo accuracy still at v5.59 levels, most users will pick manual most of the time and a chooser would tax the common case for the rare case. (2) Swapped `[minus, input, plus]` DOM order to `[plus, input, minus]` on chip cards — under inherited RTL the row renders visually as `[− | count | +]` (minus left, plus right) instead of the previous reversed-feeling layout. Added a code comment so future readers don't try to "fix" it. (3) Hebrew `chips.numpadNext` arrow flipped from `→` to `←` — dots progress right-to-left in RTL (active dot at chipIndex=0 is visually rightmost, advancing moves it leftward), so `←` is the semantically correct "forward" arrow. English `'Next →'` kept as-is (LTR document direction when English locale active per `App.tsx` line 1553).
- **v5.60.0 — Schedule top-level route + rich single-date home card** (2026-05-13): Two streams shipped together. (1) The schedule/poll UI moved from `Settings → Schedule tab` into its own top-level `/schedule` route. Rationale: Settings is for "one-time setup and monitoring" while the schedule is a recurring active surface that members hit often — burying it inside a tab made the navigation feel buried. All vote deep-links from external surfaces (push notifications via `api/notification-worker.ts`, scheduled emails via `src/utils/scheduleNotifications.ts`, OAuth redirect handling in `src/hooks/useSupabaseAuth.ts`, the in-app `VoteReminderBanner`) now point to `/schedule?pollId=…`. The legacy `/settings?tab=schedule` URL pattern gets a transparent redirect in `SettingsScreen.tsx` so old emails and push payloads still resolve correctly. Home empty-state is now role-aware (admin → amber "open the next poll" CTA; member → calm "next vote opens soon") and auto-schedule-aware (when `scheduleAutoCreateEnabled` is set, the subtitle shows the concrete next `dayName / date / time` instead of vague "soon" — uses the exported `computeNextScheduledTrigger` from `ScheduleTab.tsx`). (2) Single-date open polls now render the same rich `renderRichDateCard` helper that the pinned/confirmed state uses — date + countdown + location + `מגיעים: …` attendee list — instead of the lean per-date glance-rows view that's only meaningful when there are multiple options to compare. The card auto-elevates from `🗳 הצבעה פתוחה` to `🎯 ערב פוקר נקבע` the moment seat count hits target (de-facto pinned, even before formal admin action). Non-voters who are linked to a player see an amber `מחכים להצבעה שלך · ` subtitle prefix; that nudge is suppressed once the poll fills (nobody is actually waiting at that point). The trigger covers both `open` AND `expanded` status (the latter is just "open + wider audience" so the rich view should persist across the auto-promotion). Honest-title-reflects-state rule: title always describes the actual poll state, never overstates. The original `if (isConfirmed)` rendering was refactored into the same helper with `elevateWhenFilled: false` to keep its output bit-identical. Audit pass identified and fixed a stale-cache trap in the filled-detection logic (was gating on `comingNames.length >= targetPlayerCount`, which could under-count during a brief realtime sync window when a yes-voter's player record hadn't loaded yet — now trusts the authoritative `missingSeats === 0` signal).
- **v5.59.0 — photo chip-counting full rebuild** (2026-05-10): replaced the v5.49 multi-shot consensus pipeline (which was producing confidently-wrong counts with a buggy sign-inverted dashboard label) with a per-stack divide-and-conquer architecture. Each detected stack region gets its own LLM call (chip selfie attached as few-shot reference) PLUS three independent geometric methods (`src/utils/chipGeometry.ts`: bottom-chip self-cal, gradient counting, cross-stack shared-cal) PLUS a weighted vote in `combineLLMAndGeometry`. Stack region detection moved fully client-side (`src/utils/stackDetection.ts`) with white-stripe density primary + edge-density fallback + position-only last resort, and emits `detectionSignal` into the new `pipeline_meta` JSONB column. White balance correction (`whiteBalanceFromStripes`) sampled from the mat between stacks. Total-value sanity check (`applyTotalValueSanityCheck`, live-game only) reconciles a single low-confidence stack when summed value is off by exactly N chips of a denomination. New `ChipDetectionOverlay` component renders bounding boxes + counts; `ChipEntryScreen` got a "↻ Clear AI suggestions" link to abandon a photo result while preserving per-field user edits + a 🛟 "adjusted" badge when the sanity check fired. Dashboard added a "🛠️ pipeline health" block: pctVerifiedByMultipleMethods, avgAgreementScore, pctWhiteBalanceApplied, totalValueAdjustmentsFired, detectionSignalCounts. Manual flow is byte-identical when the photo button isn't tapped (verified via stack=undefined gating). The auto-tuner remains the active improvement lever (`tuneChipCountStrategy` rewrites only the per-stack prompt block; geometric methods + detection are deterministic code, not LLM-tunable).
- **v5.58.0 combined release — three streams merged** (2026-05-10): chip-counting permissions opened to admins (test card + dashboard, tune+revert stay owner-only), trivia template kill-switch landed (super-admin can permanently exclude bad questions, restorable), trivia leaderboard re-sorted accuracy-first, "My stats" card removed from trivia landing, AI personality traits dialed back (≤1–2 players surfaced per call, ~70% inclusion probability, prompts re-tuned to lean 90/10 game-stats vs personal facts in TTS pool — was 80/20).
- **Photo chip counting accuracy + feedback loop** (v5.47.0 → v5.49.x → v5.53.0): full evolution from "snap photo → AI proposes counts" (v5.47) through the multi-shot consensus + computed confidence rebuild (v5.49) to the in-app feedback capture infrastructure (v5.53, migration 069). Position-based identity (chips sorted ascending) replaces color discrimination as the accuracy lever, MAX aggregation across 3 parallel shots counters the systematic undercount bias, and silent diff capture in `markPlayerDone` now records (AI vs real) per-stack diffs to `chip_count_feedback` so future tuning can be empirical instead of intuitive.
- **Trivia hardening + UX polish** (v5.50.x → v5.53.0): stale-state click-time correctness lock (v5.50.2), difficulty moderation with 20-game eligibility floor and bucketed range answers (v5.51), trivia mode redesign — Group=broad, עליי=personal, Mixed=50/50 per-question (v5.52), and same-release tweaks to the report-problem button label + wider `numericDistractors` spread (0.55 → 0.85) so ±40% ballpark estimates win.
- **Home/schedule UX polish + new-group onboarding** (v5.45.0–v5.46.0): home dashboard forward-looking teaser for brand-new groups; schedule card empty state rewritten; schedule tab empty state shows next auto-create anchor; polls auto-link to games and auto-archive when their game completes; activity log shows live session minutes; observer mode for super admins viewing foreign groups.
- **Data-integrity hardening** (v5.44.6): row-level `BEFORE DELETE` guard on `game_players` for completed games + parent-existence-based cascade detection. SQL migrations `050` + `051`. Resolved a 5-day-latent bug in 043 where `pg_trigger_depth()` returned the wrong value in AFTER-STATEMENT context.
- **Realtime cache-recovery on tab return** (v5.47.0): `useRealtimeRefresh` accepts an optional `forceRefreshOnReturn` callback so screens whose underlying tables get write-heavy from peers (votes, member adds) actually re-fetch from Supabase when the tab regains focus, instead of just re-rendering stale in-memory cache.
- **Notification volume tuning** (v5.43.x → v5.44.0): cut email types, EmailJS quota system, push-only for chatty events. Owner manages quota config in Settings → Services.
- **Home dashboard rebuild** (v5.38–v5.41): pure dashboard with deep-links, gender-aware verbs, RTL emails, mobile polish.
- **Schedule / poll system** (v5.37): poll reminders, join-request banner with player linking on accept.

## Tooling capabilities you already have (do NOT ask the user, do NOT defer)

- **Supabase MCP — full READ AND WRITE access** to the live DB (project `ursjltxklmxmapfvkttj`, full privileges via PAT in `.cursor/mcp.json`). The server appears as **`supabase`** or legacy **`project-0-Poker Game-supabase`** — same project, use whichever your tool list shows. **Never** ask Lior to run a `SELECT` you can run yourself with `execute_sql`. **Never** silently fall back to "ask the user" if the MCP is missing — say so and instruct a Cursor reload.
  - Reads: `list_tables` (verbose), `execute_sql`, `list_migrations`, `get_logs`, `get_advisors`, `list_branches`, `list_edge_functions`, `generate_typescript_types`, `search_docs`.
  - Writes: `apply_migration` — but ONLY after authoring `supabase/0XX-name.sql` on disk (file IS the audit trail). No ad-hoc DDL/DML inside `execute_sql`.
  - Risky writes (`DROP`, `DELETE` w/o tight `WHERE`, RLS toggles, role grants, column drops, anything mutating user data) require explicit user confirmation FIRST. Idempotent additions (`CREATE OR REPLACE`, `IF NOT EXISTS`, `ADD COLUMN IF NOT EXISTS`) just apply, then verify with a `SELECT`.
  - Full reference: `AGENTS.md` → "Supabase MCP — Read AND Write Enabled" + `.cursor/rules/supabase-migration.mdc` + `.cursor/rules/confirm-before-risky.mdc`.

## Things to know that aren't in `AGENTS.md` or `.cursor/rules/`

- `temp_prompt.txt`, `pool-full-dump.txt`, `*.cjs` validation scripts in repo root are intentional dev artifacts. Don't delete unprompted.
- `Poker results.xlsx` and `poker-export-*.xlsx` are real user data exports. Treat as sensitive — never commit modifications.
- **`pg_trigger_depth()` does NOT increment for FK CASCADE in AFTER-STATEMENT trigger context** — see `LESSONS.md` 2026-05-08. It DOES work in BEFORE-ROW context. Migration 043 was authored on the wrong assumption and was latently broken for 5 days. When designing trigger logic that needs to distinguish cascade from direct delete, use parent-existence check, not `pg_trigger_depth()`.
- **"Forward-only" is the user's default cleanup preference**: when fixing labels, route names, classifications, or any displayed-from-historical-data field, change the WRITE path so new entries are correct and let old rows age out. Never backfill, never auto-fix existing rows. (Surfaced again in v5.45.0 around the activity log "/new-game" → "Home" rename.)
- **Hebrew copy needs care**: dual forms ("שלשום"), avoid bare prepositions ("ל" without infinitive), prefer warm forward-looking verbs over formal/scheduling words. The user pushes back hard on awkward Hebrew. When in doubt, prefer simple, inviting phrasing over technically-correct-but-stiff phrasing.

## Where the agent memory lives

```
docs/agent/
  CONTEXT.md   ← you are here (refresh in place)
  SESSIONS.md  ← chronological journal (newest first, append-only)
  LESSONS.md   ← incident → lesson, earned not pre-populated
.cursor/rules/
  agent-memory.mdc  ← enforces start/end rituals
```

The standing rules (`.cursor/rules/*.mdc`) and project map (`AGENTS.md`) are still the source of truth for **how things should be done**. This folder is the source of truth for **what's happening right now and what we've learned the hard way**.
